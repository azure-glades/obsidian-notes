A way of representing probabilities of a set of random variables using DAGs.

it basically has the bayesian probabilities. 

If Z has no parents then we know P(Z)

If A has a parent B, then we know P(A | B). we can get P(B | A) since we know P(A|B) and P(B). goal is P(B|A) anyway
